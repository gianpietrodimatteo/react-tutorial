Display the location for each move in the format (col, row) in the move history list. OK
Bold the currently selected item in the move list. OK
Rewrite Board to use two loops to make the squares instead of hardcoding them. OK
Add a toggle button that lets you sort the moves in either ascending or descending order. OK
When someone wins, highlight the three squares that caused the win. OK
When no one wins, display a message about the result being a draw. OK

Style up. OK
Add tests to the application.

Enable Single Player.
